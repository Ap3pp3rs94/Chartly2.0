<##>
# Chartly 2.0 deploy environment template
#
# Copy/paste and set these in your shell or CI secret store.
<##>

# Container registry (if applicable)
# $env:CHARTLY_REGISTRY = "ghcr.io/your-org"

# Image tag to deploy (e.g., commit SHA)
# $env:CHARTLY_IMAGE_TAG = "<sha>"

# Cluster name/identifier (if applicable)
# $env:CHARTLY_CLUSTER = "dev-cluster"

# Namespace (if applicable)
# $env:CHARTLY_NAMESPACE = "chartly"

# Prod safety gate (required for prod deployments)
# $env:CHARTLY_ALLOW_PROD_DEPLOY = "1"
